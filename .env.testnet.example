# Phase 3 — testnet / hybrid (do not commit real secrets)
# Copy to a local .env (gitignored) and export before running future testnet scripts.

TESTNET_RPC_URL=
TESTNET_CHAIN_ID=
# Never commit a populated private key. Use a CI secret or local-only .env.
TESTNET_PRIVATE_KEY=
KARMA_NONCUSTODIAL_CONTRACT_ADDRESS=
ERC20_TOKEN_ADDRESS=
SETTLEMENT_MODE=offchain
