/**
 * Optional local override for WalletConnect Cloud project id.
 * Prefer deployment-generated `public-config.json` (same directory as web3-login.html);
 * this file is merged first, then `public-config.json` overwrites when present.
 * @see public-config.json.example
 */
window.KARMAPAY_WC_PROJECT_ID = "";
