# CSP for WalletConnect sign-in (`web3-login.html`) — validate connect-src in staging (WalletConnect hosts change).
# Prefer Report-Only first. Do not apply portal CSP here.
#
# add_header Content-Security-Policy "default-src 'none'; base-uri 'self'; frame-ancestors 'none'; object-src 'none'; img-src 'self' data: blob:; style-src 'self' 'unsafe-inline'; script-src 'self' https://cdnjs.cloudflare.com https://esm.sh; font-src 'self' data:; connect-src 'self' https://cdnjs.cloudflare.com https://esm.sh https://fonts.googleapis.com https://*.walletconnect.com wss://*.walletconnect.com https://*.walletconnect.org wss://*.walletconnect.org https://registry.walletconnect.com https://rpc.walletconnect.org https://pulse.walletconnect.org; upgrade-insecure-requests" always;
