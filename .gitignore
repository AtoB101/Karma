# Build and local artifacts
/cache/
/out/
/broadcast/

# Local forge / demo outputs (do not commit)
/deployment.json
/abis.json

# Local reports and sync outputs (never commit secrets here)
/results/

# Secrets and local env (never commit real keys)
# Catch-all for all .env variants; whitelist examples only
**/.env*
!**/.env.example
!**/.env.*.example
*.pem
*.key
docker-compose.override.yml

# Python tooling / coverage
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Python bytecode
**/__pycache__/
*.pyc

# Build artifacts (never commit)
out/
cache/
broadcast/
.certora_internal/

# Test databases
*.db
*.db-shm
*.db-wal
*.db-journal

# Archives
*.zip
*.tar.gz

# Node / Playwright local artifacts
node_modules/
test-results/
playwright-report/
packages/karma-runtime-sdk/dist/

# Agent Guard: runtime public config (generated on server / CI; do not commit production ids)
apps/agent-service-guard/frontend/public-config.json
apps/console/config.js
.gas-snapshot
