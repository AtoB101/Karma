# Build and local artifacts
/cache/
/out/

# Local reports and sync outputs (never commit secrets here)
/results/

# Python bytecode
**/__pycache__/
*.pyc

# Node / Playwright local artifacts
node_modules/
test-results/
playwright-report/

# Agent Guard: runtime public config (generated on server / CI; do not commit production ids)
apps/agent-service-guard/frontend/public-config.json
