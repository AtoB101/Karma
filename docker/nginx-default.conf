server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location = / {
        try_files /apps/console/index.html =404;
    }

    location ^~ /apps/ {
        try_files $uri $uri/ =404;
    }

    location = /console {
        return 302 /apps/console/index.html;
    }
}
