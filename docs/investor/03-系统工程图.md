# 03 · 系统工程图

## 分层总览

```mermaid
flowchart TB
  subgraph Clients["接入层"]
    OC[OpenClaw / OpenManus]
    SDK[Python / TS SDK]
    CON[Operator Console]
    A2A[A2A / MCP Bridge]
  end

  subgraph PublicAPI["公开 API · FastAPI"]
    RT["/runtime · Runtime Gateway"]
    V1["/v1/* · capacity vouchers trade settlement evidence ..."]
  end

  subgraph Services["服务层"]
    SVC[services/* 业务逻辑]
    ADP[settlement_adapter → Bilateral only]
    EV[evidence_runtime]
    WRK[worker · Celery]
  end

  subgraph Chain["karma-core 链上"]
    KB[KarmaBilateral]
    SUP[AuthToken · KYA · Evidence · Scoring · Verifier · CircuitBreaker · SBT · Gateway]
  end

  subgraph Private["私有域 · 不在本公开仓"]
    ENG[风险 / 反作弊引擎 Karma2]
  end

  subgraph Economy["可选 · karma-economy 独立仓"]
    FB[FeeBridge / Treasury]
  end

  Clients --> PublicAPI
  PublicAPI --> Services
  Services --> Chain
  V1 -.->|verify 代理| ENG
  KB -.->|feeBridge 可选| FB
```

## 仓库模块地图（投资人可扫一眼）

| 模块 | 路径 | 作用 |
|------|------|------|
| 链上协议 | `karma-core/contracts/` | Bilateral + 配套合约 |
| HTTP API | `api/` | 公开商业接口 |
| 业务逻辑 | `services/` | 结算适配、交易、x402… |
| 证据运行时 | `evidence_runtime/` | Receipt / Bundle / AP2 |
| Console | `apps/console/` | 运营台（静态 HTML/JS） |
| BFF | `apps/karma_bff/` | 编排器 HMAC 集成 |
| SDK | `sdk/` · `packages/sdk` · `packages/karma-sdk` | HTTP + 链上客户端 |
| Agent 桥 | `packages/karma-openclaw` 等 | 生态接入 |
| 验证节点 | `decentralized_verifier/` | Attestation / challenge |
| 部署 | `deploy/` · `docker/` | Sepolia / Compose |

## 前后端实现关系

```mermaid
flowchart LR
  subgraph FE["前端 · apps/console"]
    OV[Overview]
    PAY[Payments / Receiving]
    TR[Trade]
    EV[Evidence / Disputes]
    AG[Agents / OpenClaw]
  end

  subgraph BE["后端 · api + services"]
    API[FastAPI routes]
    DB[(Postgres)]
    CH[Chain Adapter]
  end

  subgraph ON["链上"]
    BIL[KarmaBilateral]
  end

  FE -->|karma-public-api.js · console-actions.js| API
  API --> DB
  API --> CH
  CH --> BIL
```

### 前端实现程度（摘要）

| 页面 | 实现程度 |
|------|----------|
| Overview / Receiving / Payments / Trade | ✅ 对真实 API 实写（last-mile） |
| Evidence / Disputes / Agents / Settings | ✅ 已接线 |
| OpenClaw Connect / Cyber Console | ✅ |
| Dashboard / Verifier Explorer | ⚠️ 可 live，断线时有 mock 回退 |
| Console 内 MetaMask EIP-712 | ❌ 明确 follow-up（签名常由操作者粘贴） |
| BFF 浏览器写操作 | ❌ 只读面板 |

### 后端实现程度（摘要）

| 域 | 路由前缀 | 状态 |
|----|----------|------|
| 结算 / 容量 / 凭证 | `/v1/settlement` `/v1/capacity` `/v1/vouchers` | ✅ |
| 交易 / 支付码 / x402 / Intent | `/v1/trade` `/v1/payment-codes` `/v1/x402` `/v1/payment-intents` | ✅ |
| 证据 / 收据 / Bundle / Verify | `/v1/evidence` `/receipts` `/bundles` `/verify` | ✅ |
| Agent P1–P8 标准面 | agents / confirmations / delivery / reputation… | ✅ |
| OpenClaw / Discovery / Orchestration | `/v1/openclaw` `/discovery` `/orchestration` | ✅ |
| 安全与 Admin | `/v1/security` `/v1/admin` | ✅ |
| Verify → 私有引擎 | 代理边界 | ✅ 公开仓不含引擎权重 |

## 安全边界（必须对投资人讲清）

```mermaid
flowchart LR
  PUB[公开仓 Karma] -->|可验证结算与证据 schema| WORLD[开发者 / 审计]
  PUB -->|verify 转发| PRIV[私有运行时]
  PRIV -->|欺诈权重 · 仲裁打分| PRIV
  PUB -.->|不包含| LEADS[客户线索 / 私密策略]
```

详见：`docs/security-boundary.md` · `VISIBILITY_MAP.md`
