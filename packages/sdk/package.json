{
  "name": "@karma-network/sdk",
  "version": "0.2.0",
  "private": true,
  "description": "TypeScript SDK for Karma public HTTP API (P0–P1: capacity, vouchers, settlement, receipts + typed receipt extensions)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "license": "AGPL-3.0-only",
  "keywords": ["karma", "agents", "settlement"],
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
