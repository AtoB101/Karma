# karma_billing

Off-chain billing / receipt sync helpers.

**Sepolia pilot scope:** not required. The invite pilot rail is **KarmaBilateral** on Sepolia
via `packages/karma-sdk` (see `docs/PILOT_E2E_PATH.md`).

Solana Merkle anchoring (`bridge.py` → `packages/karma_solana`) is deferred and incomplete.
Do not treat this package as part of the public pilot integration surface.
